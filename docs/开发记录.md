# kiaao 开发记录

## 项目概况

**kiaao**：超轻量级、零编译、无虚拟 DOM 的响应式前端框架。

- 核心概念：4 个（define / derive / effect / h）
- 总 API：10 个（+ mount / unmount / onMount / onUnmount / Show / List）
- 设计哲学：显式选择器订阅，纯运行时细粒度更新
- 代码量目标：约 300-350 行

## 当前状态（Phase 3 完成）

### 文件结构

```
src/
  index.ts      → 公开 API 入口（10 个导出）
  types.ts      → Symbol 常量 + 类型定义
  runtime.ts    → 核心响应式运行时（define/effect/derive）
  dom.ts        → DOM 渲染 + 控制流 + 生命周期（h/Show/List/mount/unmount/onMount/onUnmount）
tests/
  index.test.ts → 核心运行时测试（14 个）
  dom.test.ts   → DOM 渲染测试（31 个）
docs/
  spec.md       → 框架规范 v1.4
  开发记录.md    → 本文件
```

### 测试覆盖

**总计 45 个测试，全部通过。**

| 模块           | 用例数 | 覆盖场景                                                            |
| -------------- | ------ | ------------------------------------------------------------------- |
| `define`       | 5      | 基础读写、setter 函数模式、selector 派生函数、selector 就地计算     |
| `effect`       | 5      | 立即执行、信号变化重跑、隔离、stop、精准订阅                        |
| `derive`       | 4      | 派生计算、上游更新、IS_REACTIVE 标记、selector 上游                 |
| `h` DOM 模式   | 6      | 标签、属性、事件、静态子节点、style、数组展平                       |
| `h` 响应式绑定 | 4      | getter 绑定、信号更新、精准订阅、derive 绑定                        |
| `h` 组件模式   | 2      | 基础组件、嵌套组件                                                  |
| 生命周期       | 4      | onMount、onUnmount、effect 清理、动态绑定清理                       |
| mount/unmount  | 1      | 挂载到容器并卸载                                                    |
| `Show`         | 6      | 渲染、fallback、切换、普通函数 when、动态内容生命周期               |
| `List`         | 7      | 渲染、更新、节点复用、删除、排序、新增条目生命周期                  |
| -------------- | ------ | ------------------------------------------------------------------- |
| `define`       | 5      | 基础读写、setter 函数模式、selector 派生函数、selector 就地计算     |
| `effect`       | 5      | 立即执行、信号变化重跑、无关信号隔离、stop 永久取消、选择器精准订阅 |
| `derive`       | 4      | 派生计算、上游更新、IS_REACTIVE 标记、selector 上游                 |
| `h` DOM 模式   | 6      | 标签创建、属性设置、事件绑定、静态子节点、style、数组展平           |
| `h` 响应式绑定 | 4      | getter 动态绑定、信号更新、精准订阅、derive 绑定                    |
| `h` 组件模式   | 2      | 基础组件、嵌套组件（children 合并）                                 |
| 生命周期       | 4      | onMount、onUnmount、effect 清理、动态绑定清理                       |
| mount/unmount  | 1      | 挂载到容器并卸载                                                    |

### 已实现功能

| API                           | 文件       | 状态                                                        |
| ----------------------------- | ---------- | ----------------------------------------------------------- |
| `define<T>`                   | runtime.ts | ✅ 完成                                                     |
| `effect(fn)`                  | runtime.ts | ✅ 完成（含 stop 函数、组件自动注册）                       |
| `derive<T>`                   | runtime.ts | ✅ 完成                                                     |
| `h(tag, props?, ...children)` | dom.ts     | ✅ 完成（双模式 + children 展平 + 组件 children 合并）      |
| `onMount(fn)`                 | dom.ts     | ✅ 完成                                                     |
| `onUnmount(fn)`               | dom.ts     | ✅ 完成                                                     |
| `mount(root, container)`      | dom.ts     | ✅ 完成                                                     |
| `unmount(root)`               | dom.ts     | ✅ 完成                                                     |
| `<Show>`                      | dom.ts     | ✅ 完成（条件渲染，支持响应式/普通函数 when，分支完全重建） |
| `<List>`                      | dom.ts     | ✅ 完成（列表渲染，key 基 DOM 复用，新增/删除/重排序）      |

### 关键实现决策

- **Getter 设计**：`getter(selector)` 返回 `() => R` 响应式函数（非立即求值），挂载 `IS_REACTIVE` Symbol
- **依赖追踪**：信号内部 `Map<selectorFn, Set<DepEntry>>`，effect 维护 `ownedDeps` 双向引用（Vue 3 范式）
- **清理机制**：effect 重跑前清理旧 dep、重跑中重新收集；stop 函数从所有信号中移除条目
- **derive**：内部用 `define` 做版本信号，内部 `effect` 监听上游，值不变时跳过版本 bump
- **notifySignal 安全**：快照 deps 后再迭代，避免 effect 重跑时修改 Map 导致死循环
- **组件生命周期**：`h(component)` 创建实例压栈，`onMount/onUnmount` 注册到栈顶实例，`mount/unmount` 递归遍历 DOM 树触发

## 阶段计划

| 阶段 | 内容                                                       | 状态      |
| ---- | ---------------------------------------------------------- | --------- |
| 一   | types.ts + runtime.ts（define/effect/derive + 全局上下文） | ✅ 完成   |
| 二   | dom.ts（h/onMount/onUnmount/mount/unmount）                | ✅ 完成   |
| 三   | dom.ts（Show/List 控制流）                                 | ✅ 完成   |
| 四   | packages/example 改造为 kiaao API                          | ⏳ 待开始 |

## 开发日志

### 2026-06-06 Phase 3

- 实现 `Show` 条件渲染组件
  - `when` 支持响应式函数（IS_REACTIVE）和普通函数
  - 分支被完全销毁/重建，通过 `disposeNode` 清理
  - 初次渲染使用 DocumentFragment 兜底 parentNode 为 null 的问题
  - 动态插入内容调用 `triggerMount` 触发生命周期
- 实现 `List` 列表渲染组件
  - 基于 key 的 DOM 节点复用（新增/删除/重排序均正确处理）
  - 新增条目调用 `triggerMount` 触发 onMount
  - 移除条目通过 `disposeNode` 触发 onUnmount
- 新增 13 个测试（Show 6 个 + List 7 个），覆盖：
  - Show：渲染/切换/普通函数 when/动态内容挂载/内容卸载
  - List：渲染/更新/节点复用/删除/排序/动态内容挂载/卸载

### 2026-06-06 Phase 2

- 实现 `h()` 双模式（DOM mode + Component mode）
  - DOM mode：createElement、props 设置（属性/事件/style）、children 处理（展平/响应式绑定/静态文本）
  - Component mode：创建实例、压/出栈、合并 rest children 到 props
- 实现 `onMount()` / `onUnmount()` — 通过组件实例栈注册
- 实现 `mount()` / `unmount()` — 递归遍历 DOM 树触发生命周期
- effect 注册 stop 到当前组件实例，卸载时统一清理
- 解决 `notifySignal` Map 迭代修改死循环问题（快照方案）
- 添加 `happy-dom` 依赖，DOM 测试使用 `@vitest-environment happy-dom`
- 新增 `tests/dom.test.ts`（18 个测试）

### 2026-06-06 Phase 1

- 创建 `src/types.ts`：Symbol 常量、公开类型（Getter/Setter/ReactiveFunction）、内部类型（ComponentInstance）
- 创建 `src/runtime.ts`：define、effect、derive 完整实现
  - define：信号创建、getter(selector) 返回响应式函数、setter 双模式、更新对账
  - effect：栈结构 support 嵌套、ownedDeps 双向清理、返回 stop 函数
  - derive：内部 effect 监听上游、版本信号通知下游、值不变跳过更新
- 更新 `src/index.ts` 导出
- 新增 `tests/index.test.ts`（14 个测试）
