# 清理清单

**状态**：待 v2 稳定后执行
**日期**：2026-07-24

v2 全量测试通过、文档迁移完成后，等用户确认后清理。

## 待删除文件

### 源码

```text
src/router/v1/index.ts
src/router/v1/utils.ts
src/router/index.ts        # 修改为 re-export v2 后可保留入口
```

### 测试

```text
tests/router/v1/router.test.ts
tests/router/v1/router-extreme.test.ts
```

### 文档

```text
docs/路由/路由API方案.md    # v1 文档，v2 稳定后删除
```

## 临时保留

```text
src/router/v2/             # v2 主体
src/router/index.ts        # re-export v2（可以保留作为对外入口）
tests/router/v2-*.test.ts  # v2 测试
guide/router.md            # v2 文档
```

## 删除顺序

1. 先确认 `vp test` 全绿且 v2 测试覆盖 v1 等价行为
2. 删除 `docs/路由/路由API方案.md`
3. 删除 `src/router/v1/`
4. 删除 `tests/router/v1/`
5. 更新 `docs/路由/v2实施/` 中的状态标记

## 验证

```bash
vp test
vp check
```

预期：

- 测试通过
- 编译/类型检查通过
- 没有 v1 相关引用遗留
