# 箭头函数替换计划

**条件**：`function` 语法、体 ≤ 2 条语句、无 `this` 引用

---

## 替换清单（13 个函数，按文件分组）

### `src/reactive/core.ts`（3 个）

| 行  | 当前                                                                                  | 替换为                                                                                   |
| --- | ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| 22  | `export function setRenderMode(mode: RenderMode): void { currentRenderMode = mode; }` | `export const setRenderMode = (mode: RenderMode): void => { currentRenderMode = mode; }` |
| 26  | `export function getRenderMode(): RenderMode { return currentRenderMode; }`           | `export const getRenderMode = (): RenderMode => currentRenderMode;`                      |
| 36  | `export function isUse(v: any): v is Getter<any> { return isNotNil(v) && ...; }`      | `export const isUse = (v: any): v is Getter<any> => isNotNil(v) && ...;`                 |
| 49  | `export function toValue(v: any): any { return isUse(v) ? (v as any)() : v; }`        | `export const toValue = (v: any): any => isUse(v) ? (v as any)() : v;`                   |

### `src/dom/directive.ts`（1 个）

| 行  | 当前                                                                                                  | 替换为                                                                                        |
| --- | ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| 39  | `export function isDirective(fn: any): boolean { return isFunction(fn) && fn[DIRECT_KEY] === true; }` | `export const isDirective = (fn: any): boolean => isFunction(fn) && fn[DIRECT_KEY] === true;` |

### `src/dom/dom-utils.ts`（3 个）

| 行  | 当前                                                                                                                        | 替换为                                                                                                                         |
| --- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| 6   | `export function splitSet(str: string): Set<string> { return new Set(str.trim().split(/\s+/)); }`                           | `export const splitSet = (str: string): Set<string> => new Set(str.trim().split(/\s+/));`                                      |
| 50  | `export function stripPrefix(rawKey: string): { prefix: ...; key: string } { const prefix = ...; return { prefix, key }; }` | `export const stripPrefix = (rawKey: string): { prefix: ...; key: string } => { const prefix = ...; return { prefix, key }; }` |
| 88  | `export function escapeHtml(s: string): string { return s.replace(...); }`                                                  | `export const escapeHtml = (s: string): string => s.replace(...);`                                                             |

### `src/dom/fragment.ts`（1 个）

| 行  | 当前                                                                                                                                 | 替换为                                                                                                                       |
| --- | ------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------- |
| 6   | `export function Fragment(props: { children?: any }): Node { return h("div", { style: { display: "contents" } }, props.children); }` | `export const Fragment = (props: { children?: any }): Node => h("div", { style: { display: "contents" } }, props.children);` |

### `src/dom/ssr-helpers.ts`（4 个）

| 行  | 当前                                                                                                          | 替换为                                                                                                |
| --- | ------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| 24  | `export function ssr(text: string): SSRSafe { return { [SSR_MARKER]: true as const, html: text }; }`          | `export const ssr = (text: string): SSRSafe => ({ [SSR_MARKER]: true as const, html: text });`        |
| 28  | `export function isSSRSafe(v: any): v is SSRSafe { return v && v[SSR_MARKER] === true && isString(v.html); }` | `export const isSSRSafe = (v: any): v is SSRSafe => v && v[SSR_MARKER] === true && isString(v.html);` |
| 37  | `export function isMappingTable(v: any): boolean { return isPlainObject(v) && !isSSRSafe(v); }`               | `export const isMappingTable = (v: any): boolean => isPlainObject(v) && !isSSRSafe(v);`               |
| 45  | `export function isVoidElement(tag: string): boolean { return VOID_ELEMENTS.has(tag); }`                      | `export const isVoidElement = (tag: string): boolean => VOID_ELEMENTS.has(tag);`                      |

---

## 注意事项

- **`setRenderMode`**：无 return，用块体 `{}`
- **`ssr`**：返回对象字面量，需包裹 `({ ... })`
- **`stripPrefix`**：2 条语句，用块体
- **`Fragment`**：组件函数，无 `this`，安全替换

## 实施结果 ✅

| 文件                 | 函数                                                  | 状态 |
| -------------------- | ----------------------------------------------------- | ---- |
| `reactive/core.ts`   | `setRenderMode`, `getRenderMode`, `isUse`, `toValue`  | ✅   |
| `dom/directive.ts`   | `isDirective`                                         | ✅   |
| `dom/dom-utils.ts`   | `splitSet`, `stripPrefix`, `escapeHtml`               | ✅   |
| `dom/fragment.ts`    | `Fragment`                                            | ✅   |
| `dom/ssr-helpers.ts` | `ssr`, `isSSRSafe`, `isMappingTable`, `isVoidElement` | ✅   |

`vp check src/`: 0 错误
`vp test`: 26 文件 / 631 通过
