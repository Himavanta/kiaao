# 遗留问题处理跟踪

## 问题清单

| 优先级 | 问题                       | 类型     | 状态    |
| ------ | -------------------------- | -------- | ------- |
| 🔴 P0  | `context.use` 代码重复     | 重构     | ✅ 完成 |
| 🟡 P1  | `h()` 类型签名缺失指令重载 | 类型增强 | ✅ 完成 |
| 🟡 P2  | `ssr-helpers.ts` 拆分      | 文件拆分 | ✅ 完成 |
| 🟡 P3  | `directives.ts` 拆分       | 文件拆分 | 待开始  |

---

## P0：消除 `context.use` 代码重复

**问题**：`h.ts` 的 `createContextUse` 与 `directive.ts` 的 `context.use` 有 8 行完全相同的模式（调用 `use` → 引用检查 → 注册 stop）。

**方案**：提取共享函数 `registerSignalStop`。

**改动文件**：

- `src/reactive/core.ts` — 新增 `registerSignalStop`
- `src/dom/h.ts` — `createContextUse` 使用共享函数
- `src/dom/directive.ts` — `context.use` 使用共享函数

### 待办

- [ ] 在 `core.ts` 新增 `registerSignalStop`
- [ ] 更新 `h.ts` 的 `createContextUse`
- [ ] 更新 `directive.ts` 的 `context.use`
- [ ] 验证 `vp check` + `vp test`

---

## P1：`h()` 类型重载支持指令函数

**问题**：`h()` 签名 `string | ((props, context?) => any)` 不包含 `DirectiveFunction`，导致测试中 `h(TestDir as any, null, ...)`。

**方案**：为 `h()` 添加函数重载。

**改动文件**：

- `src/dom/h.ts` — 添加重载签名

### 待办

- [ ] 为 `h()` 添加 `DirectiveFunction` 重载
- [ ] 测试文件中移除 `as any` 转型
- [ ] 验证 `vp check` + `vp test`

---

## P2：拆分 `ssr-helpers.ts`

**问题**：250 行，混合 5 种职责。

**方案**：拆分为 `ssr/serialize.ts`、`ssr/render.ts`，原文件作为统一入口。

**改动文件**：

- `src/dom/ssr/serialize.ts`（新建）
- `src/dom/ssr/render.ts`（新建）
- `src/dom/ssr-helpers.ts`（精简）

### 待办

- [ ] 创建 `ssr/serialize.ts`：`serializeCssText`、`serializeAttr`、`serializeAttrs`、`stripDirectives`
- [ ] 创建 `ssr/render.ts`：`hSSR`、`hSSRFunctionTag`、`hSSRWhenTag`、`renderSSRChild`
- [ ] 精简 `ssr-helpers.ts`：保留 `SSRSafe`、`ssr()`、`isSSRSafe`、`isPlainObject`、`isVoidElement` + 从各子模块 re-export
- [ ] 更新 `h.ts` 的导入路径
- [ ] 验证 `vp check` + `vp test`

---

## P3：拆分 `directives.ts`

**问题**：436 行，混合 when 和 each 两种独立职责。

**方案**：拆分为 `when.ts`、`each.ts`，原文件保留为统一入口。

**改动文件**：

- `src/dom/when.ts`（新建）
- `src/dom/each.ts`（新建）
- `src/dom/directives.ts`（精简）

### 待办

- [ ] 创建 `each.ts`：`normalizeEachSource`、`syncItemSignal`、`syncCleanupRemoved`、`renderEach`、`createEachElement`
- [ ] 创建 `when.ts`：`detectWhenMode`、`renderWhenMappingMode`、`renderWhenLazyMode`、`renderWhenStaticMode`、`subscribeWhen`、`createWhenElement`
- [ ] 精简 `directives.ts`：re-export 来自 when.ts 和 each.ts 的内容
- [ ] 更新 `h.ts` 的导入路径
- [ ] 验证 `vp check` + `vp test`

---

## 进度

| 问题 | 状态    | 完成日期   |
| ---- | ------- | ---------- |
| P0   | ✅ 完成 | 2026-06-14 |
| P1   | ✅ 完成 | 2026-06-14 |
| P2   | ✅ 完成 | —          |
| P3   | 待开始  | —          |
