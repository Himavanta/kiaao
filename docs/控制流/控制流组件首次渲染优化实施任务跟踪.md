# 控制流组件首次渲染优化 — 实施任务跟踪

> 关联设计文档：[控制流组件首次渲染时机优化专题文档](./控制流组件首次渲染时机优化专题文档.md)
> 状态：🟡 规划中
> 开始日期：待定

---

## 总体策略

**逐一模块改造，每步独立验证。** 每个模块改完后运行 `vp check` + 相关测试，确认无回归后再进入下一步。遇到不确定的地方停下来讨论。

### 阶段划分

```
阶段 1: adoptBranch 增加 skipInsert 参数
阶段 2: adoptResult 自引用守卫
阶段 3: Show 组件改造
阶段 4: Case 组件改造
阶段 5: Each 组件改造
阶段 6: 清理与全面测试
```

---

## 阶段 1：`adoptBranch` 增加 `skipInsert` 参数

**文件**：`core/flow-shared.ts`

- [ ] 修改 `adoptBranch` 参数类型，增加 `skipInsert?: boolean`
- [ ] 在函数体内，当 `skipInsert === true` 时跳过 `adapter.before` 循环和 `triggerMount`
- [ ] 确保 `skipInsert` 默认 `undefined`（行为等同于 `false`）
- [ ] 确认现有调用方不受影响（`clearAllEntries`、`diffEntries`、`rebuildAllItems` 等未传 `skipInsert`，默认走 `false` 路径）
- [ ] `vp check` 通过
- [ ] `flow-shared.ts` 相关测试通过

**验证标准**：`vp check` 零错误，现有控制流测试通过。

---

## 阶段 2：`adoptResult` 自引用守卫

**文件**：`core/component.ts`

- [ ] 在 `adoptResult` 的边界路径中，挂接 Owner 前加 `hr.owner !== owner` 判断
- [ ] 确认语义：控制流组件返回 `context.owner` 时，`hr.owner === owner`，跳过自引用连接，仅返回 `hr.nodes`
- [ ] 确认正常组件（返回子组件 Owner）不受影响：`hr.owner !== owner`，正常连接
- [ ] 确认 DOM 元素（返回 `owner: null`）不受影响：走非边界路径
- [ ] `vp check` 通过
- [ ] 现有 `adoptResult` 相关测试通过

**验证标准**：`vp check` 零错误，组件 Owner 连接测试通过。

---

## 阶段 3：Show 组件改造

**文件**：`core/show.ts`

- [ ] 锚点创建改为 `const anchor = adapter.comment("show")` + `context.owner.elements.add(anchor)`，不再依赖 `initAnchor`
- [ ] 移除 `context.onMount(renderBranch)`
- [ ] 在函数体内调用 `renderBranch(true)`（首次同步渲染，`skipInsert: true`）
- [ ] 信号回调中调用 `renderBranch(false)`（后续更新，`skipInsert: false`）
- [ ] `currentResult` 为 null 时（无匹配分支），nodes = `[anchor]`
- [ ] 返回 `createHResult(context.owner, nodes, [], [])`（边界 HResult）
- [ ] 清理不再需要的 import（`initAnchor` 等）
- [ ] `vp check` 通过
- [ ] Show 测试全部通过

**验证标准**：Show 的首次渲染、信号切换、无 fallback 等场景全部正确。

---

## 阶段 4：Case 组件改造

**文件**：`core/case.ts`

- [ ] 与 Show 相同的改造模式
- [ ] 锚点创建改为 `adapter.comment("case")` + `context.owner.elements.add(anchor)`
- [ ] 移除 `context.onMount(renderBranch)`
- [ ] 函数体内调用 `renderBranch(true)`，信号回调中调用 `renderBranch(false)`
- [ ] 无匹配分支且无 fallback 时，nodes = `[anchor]`
- [ ] 返回 `createHResult(context.owner, nodes, [], [])`
- [ ] 清理不再需要的 import
- [ ] `vp check` 通过
- [ ] Case 测试全部通过

**验证标准**：Case 的首次渲染、信号切换、回退匹配全部正确。

---

## 阶段 5：Each 组件改造

**文件**：`core/each.ts`

### 5.1 `renderEachEntry` 增加 `skipInsert` 参数

- [ ] 函数签名增加 `skipInsert: boolean` 参数
- [ ] 在 `adoptBranch` 调用中传入 `skipInsert`

### 5.2 `diffEntries` 和 `rebuildAllItems` 适配

- [ ] `diffEntries` 中调用 `renderEachEntry(state, rawValue, identity, i, false)`
- [ ] `rebuildAllItems` 中调用 `renderEachEntry(state, rawValue, i, i, false)`

### 5.3 首次渲染路径

- [ ] 锚点创建改为 `adapter.comment("each")` + `context.owner.elements.add(anchor)`
- [ ] 移除 `context.onMount(sync)`
- [ ] 实现 `sync(isInitial: boolean)` 分离首次与后续逻辑：
  - [ ] `isInitial === true` + 有数据：遍历 items，`renderEachEntry(..., true)` 构建 entries
  - [ ] `isInitial === true` + 空数组 + 有 fallback：`adoptBranch({ skipInsert: true })` 渲染 fallback
  - [ ] `isInitial === true` + 空数组 + 无 fallback：无操作
  - [ ] `isInitial === false`：保持现有 diff/清理逻辑
- [ ] 函数体内调用 `sync(true)`，信号回调中调用 `sync(false)`
- [ ] 构建 `allNodes`：优先使用 `fallbackResult.nodes`，否则 `entries.flatMap(e => e.result.nodes)`
- [ ] 返回 `createHResult(context.owner, [...allNodes, anchor], [], [])`
- [ ] 清理不再需要的 import
- [ ] `vp check` 通过
- [ ] Each 测试全部通过（含空列表 fallback、keyed/non-keyed 切换、嵌套 Each）

**验证标准**：Each 的首次渲染、空数组 fallback、keyed diff、non-keyed 重建全部正确。

---

## 阶段 6：清理与全面测试

- [ ] 全局 `vp check --fix` 通过（零错误）
- [ ] 全量测试运行，确认无回归
- [ ] 构建通过
- [ ] 手动检查：控制流组件不再 import `initAnchor`、`onMount` 等不再使用的函数
- [ ] 更新任务跟踪文档状态

**验证标准**：全量测试与基线一致（418 pass / 7 fail），构建通过。

---

## 风险与暂停点

| 步骤   | 风险                                                                      | 暂停点                                                           |
| ------ | ------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| 阶段 1 | `adoptBranch` 的 `skipInsert` 默认行为是否与现有调用兼容                  | 需要确认 `clearAllEntries` 等不传 `skipInsert` 时走 `false` 路径 |
| 阶段 3 | Show 的边界 HResult 返回 `context.owner` 后自引用守卫是否生效             | 需运行测试验证 Owner 树正确性                                    |
| 阶段 5 | `diffEntries` 中 `renderEachEntry` 传 `false` 后节点插入行为              | 需要确认 `before` 在更新路径中正常工作                           |
| 阶段 5 | `repositionEntry` 读取 `owner!.elements` 在 `skipInsert: true` 后是否完整 | 理论上 `handleComponent` 内的 `adoptResult` 已注册节点           |
| 阶段 5 | 首次空数组 fallback 渲染后，更新为非空时切换是否正确                      | 需要测试 `fallbackResult.owner` 的 dispose 和重建                |
